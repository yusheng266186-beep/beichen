# 2026-09-07 验收与同步记录

运行版本：页面与云函数 v2.8.1；Redis 开启，ready=true，SENTINEL_MODE=on。
发行标签：v2.8.1-sync.20260907，表示验收后的同步快照，不移动旧 v2.8.1 标签。

## 验收证据

| 项目 | 结果 |
| --- | --- |
| 创建预检 | 规范 VPC/子网 ID，256MB/1副本通过 |
| 真实关闭 | 删除确认，readyz 503、ready=false |
| 关闭哨兵 | [34078190029](https://github.com/yusheng266186-beep/beichen/actions/runs/34078190029) 成功，明确跳过拨测 |
| 真实开启 | 自动创建并接回函数，readyz 200、ready=true、Redis 可达 |
| 开启哨兵 | [34078296738](https://github.com/yusheng266186-beep/beichen/actions/runs/34078296738) 成功，实际拨测通过 |
| 开关回归 | 14 项通过 |
| 桌面启动器 | 已配置版实际状态查询通过 |
| 正式库本地检查 | 语法、HTML、密钥扫描通过；Node 测试 12 通过、0 失败、1 Redis 测试因本地未配置而跳过；云端 Redis 另有以上真实循环证据 |

## 同步内容

正式库：README、CHANGELOG、部署指南、本记录、tools/redis-switch 全套工具。补齐已合入的历史横幅、移动端卡片、四选项、矩形图片导出、导航关闭钮与夜航提示修复的发行说明。

交接库：操作执行清单当前状态、历史验收报告追加记录、专项 Redis 验收记录、同版脚本与 Windows 启动器。

发行附件：公开可配置 ZIP、SHA-256 清单。实际配置、快照、凭据和私有交接文档不入公开附件。

桌面：北辰Redis开关 文件夹及快捷方式；配置引用本机私有文档，可直接查看、开启、关闭。

## 回归范围与边界

14 项覆盖分页/误选、重名、隔离/删除状态、SDK 地址、已有实例复用、环境变量保留、预检和创建失败、缺失快照、重建参数、无密码快照、非按量保护、网络保护、冒烟失败不启用哨兵。

off 清空云端会话、额度及防重放状态；快照只含配置。开启需要余额，命令串行执行。测试结束保持开启。本次不重复此前聊天/星图人工全流程验收。
