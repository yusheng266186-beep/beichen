# 北辰版本更新日志
## 2026-09-06 · v2.8.1（设置面板重排 · 验证码重放区分）

### 一句话结论

设置面板按信息分组重排（版本号与存档覆盖说明移至底部）；模型行文案与思考强度三档同步；思考低语开关补说明；服务端将“验证码已被本窗口消费过（重放）”与“验证码错误”区分返回，前端据此提示“这组码已经用过，请等下一组”。

## 2026-09-06 · v2.8.0（思考强度三档 · 低语优化 · 值班哨兵）

### 一句话结论

学生可在设置中选择思考强度（省时/标准/深思，服务端按枚举换算思考预算），思考低语显示稳定化并可关闭；输入新验证码自动覆盖旧对话存档；仓库新增每 5 分钟一次的 /readyz 拨测工作流（值班哨兵）。

### 1. 后端改动

- /chat/completions 新增可选枚举字段 effort（eco/standard/deep，缺省 standard）；思考预算由服务端换算：standard 沿用环境变量口径（日常 2048/星图 4096），eco 为其一半（下限 100），deep 为其两倍（上限 16384）；非法值一律 400，effort 字段不透传上游。
- 前后端版本统一 v2.8.0。

### 2. 前端改动

- 设置面板新增“思考强度”三档与各档预估星图生成时间；“思考低语”开关（默认开）。
- 思考低语改为恒定尾窗（尾部约 40 字）+ 固定行宽，不再忽长忽短。
- 每次输入新验证码自动覆盖本机旧对话存档（历次星图档案保留）。

### 3. 运维

- 新增 .github/workflows/sentinel.yml：每 5 分钟拨测 /readyz，失败邮件告警；函数地址存于仓库变量 RELAY_URL。

### 4. 验收要点

1. 默认（标准）档行为与 v2.7.0 一致；
2. 三档真机点亮星图，耗时与预估量级相符；
3. 新验证码后旧对话不恢复、历次星图保留。


## 2026-09-04 · v2.7.0（前后端统一版本）

### 一句话结论

本次更新在共享状态版本完成正式部署的基础上，把页面和后端的对外版本统一为 **v2.7.0**。此前页面一直显示“中转服务未就绪”，根本原因不是页面请求格式错误，而是云函数部署检查脚本误判了腾讯云返回结果。修正后，云函数、共享 Redis 和页面之间已经重新对齐。

### 1. 本次更新解决的问题

旧版本的会话、动态验证码使用记录、次数和重复提交记录主要保存在单个云函数实例的内存里。云函数重启、冷启动或出现多个实例时，可能出现以下问题：

- 一个实例记住了验证状态，另一个实例却不认识；
- 次数在不同实例之间不同步；
- 网络重试可能造成重复结算；
- Redis 出现问题时，页面无法明确知道服务到底能不能用；
- 云函数实际已经恢复运行，但部署脚本因为读取了一个不稳定的“代码大小”字段，误报部署超时。

### 2. 后端改动

#### 2.1 使用共享 Redis 保存关键状态

以下内容统一写入共享 Redis：

- 动态验证码的使用记录；
- 登录后的会话票据；
- 普通对话的次数占用与释放记录；
- 星图生成后的待结算状态；
- `X-Request-ID` 对应的重复提交收据。

Redis 不可用时，服务不会偷偷切回单机内存，而是拒绝新的关键请求，避免不同实例各自计算次数。

#### 2.2 保持原有页面请求方式

页面原有的聊天、星图生成和确认结算请求格式保持兼容：

- 普通聊天仍按原页面格式发送；
- 星图请求仍使用原来的高预算标记；
- 星图结算仍通过原来的 `/run/complete` 和 `X-Request-ID` 完成；
- 模型名称、上游地址和密钥仍由后端控制，浏览器不保存密钥。

#### 2.3 增加真实就绪检查

云函数提供：

- `/healthz`：检查服务是否能响应；
- `/readyz`：同时检查后端配置、上游配置和 Redis 是否可用。

只有 `/readyz` 返回完整的就绪结果，页面设置中的“检查连接”才会显示“正常”。如果页面版本和后端版本不一致，页面会提示“需同步”。

#### 2.4 修正部署脚本的误判

本次排查确认：

1. 腾讯云函数配置已经成功写入；
2. 代码上传后函数状态已经从 `Updating` 变为 `Active`；
3. 旧脚本仍然要求返回一个大于 0 的 `CodeSize`；
4. 腾讯云当前返回中这个字段可能为空或为 0；
5. 脚本因此把已经运行的函数误判成“上传失败”。

现在改为以云函数状态 `Active` 或 `Running` 作为完成依据，不再依赖这个不稳定字段。

### 3. 前端同步范围

页面只同步后端变化确实需要的部分：

- 页面版本号由 `v2.6.20` 更新为 `v2.7.0`；
- 后端对外版本由过渡标记 `v2.7.0-stateful` 统一为 `v2.7.0`；
- 页面要求的后端版本同步更新为 `v2.7.0`，避免设置页把同一正式版本误判为不一致；
- 设置页增加真实的 `/readyz` 检查；
- 显示后端版本和页面版本；
- 增加“未就绪”“需同步”“无法连接”等明确提示；
- 保持原有聊天界面、提示词、样式、消息解析和请求流程不变。

### 4. 正式部署记录

本次部署使用正式仓库 `yusheng266186-beep/beichen` 的代码，私有交接库只用于读取部署所需的受控配置。真实密钥没有写入正式仓库、README、更新日志或部署输出。

部署时沿用已经创建的腾讯云函数、Redis 和网络资源，没有另外创建第二套资源。部署包包含后端运行依赖，确保云函数启动时能够正常加载 Redis 连接库。

### 5. 实际验收结果

远程部署任务的“部署到腾讯云”步骤已成功完成，随后直接访问生产函数的 `/readyz` 进行检查，结果如下：

| 检查项目 | 实际结果 |
| --- | --- |
| HTTP 状态 | `200` |
| 服务就绪标记 | `ready: true` |
| 后端配置 | `providerConfigured: true` |
| 状态保存方式 | `redis` |
| Redis 连接 | `stateStoreReachable: true` |
| 后端版本 | `v2.7.0` |
| 页面版本 | `v2.7.0` |

这表示页面所依赖的后端配置和共享次数记录已经准备好。打开网页后，在“设置”中点击“检查连接”，预期显示“正常”。

### 6. 部署收尾

- 一次性部署工作流已从私有交接库移除；
- 临时部署压缩包已从私有交接库移除；
- 正式仓库没有新增密钥文件；
- 正式仓库原有持续集成检查仍保留；
- 本次更新不改变旧版页面存档和无关前端内容。

### 7. 用户验收方法

1. 打开 [北辰在线页面](https://yusheng266186-beep.github.io/beichen/)；
2. 进入“设置”；
3. 点击“检查连接”；
4. 确认状态显示“正常”；
5. 输入有效动态码后，再进行一次普通对话和一次星图生成测试。

如果手机仍显示旧的“未就绪”，先关闭旧页面，再重新打开网页；也可以用浏览器无痕页面打开一次，排除手机浏览器缓存影响。

### 8. 安全说明

本次公开日志只记录配置类别、版本和验收结果，不记录任何真实的密钥、验证码种子、Redis 密码或云平台身份信息。正式使用一段时间并确认稳定后，建议按常规安全流程轮换曾经放在交接材料中的云平台密钥和相关凭据。
