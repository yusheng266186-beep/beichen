{
  "name": "beichen-qianfan-relay",
  "version": "2.7.0",
  "private": true,
  "description": "北辰选科谈心的腾讯云 SCF 中转服务",
  "main": "scf-relay.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node --check scf-relay.js && node --check state-store.js",
    "test": "node --test tests/*.test.js",
    "verify": "npm run check && npm test"
  },
  "dependencies": {
    "redis": "^4.7.1"
  }
}
